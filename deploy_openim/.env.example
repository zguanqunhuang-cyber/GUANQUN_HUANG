# Copy this file to `.env` and update the values before running the helper script.
# Only plain text secrets are required here; the script base64-encodes them for
# the Kubernetes Secret manifests.

# Namespace that will contain all OpenIM resources.
OPENIM_NAMESPACE=openim

# Path to the Open-IM-Server deployment manifests relative to the repo root.
OPENIM_DEPLOY_DIR=Open-IM-Server/deployments/deploy

# Set to "true" if you want the helper script to deploy the bundled Redis,
# MongoDB, Kafka, and MinIO StatefulSets. Leave as "false" if you already have
# managed services for these dependencies.
BOOTSTRAP_INFRA=false

# --- Secrets used by the Kubernetes Secret manifests ---
# Leave empty only if the corresponding dependency does not require auth.
REDIS_PASSWORD=
MONGO_USERNAME=
MONGO_PASSWORD=
MINIO_ACCESS_KEY=
MINIO_SECRET_KEY=
# Optional. Required only when Kafka SASL is enabled.
KAFKA_PASSWORD=
